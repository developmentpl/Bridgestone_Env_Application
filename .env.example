# Postgres connection string (Neon for demo + production)
# Get one free at https://neon.tech -> create project -> copy the connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/dbname?sslmode=require"

# ---- Phase 2 only (Google SSO via Auth.js) ----
# AUTH_SECRET=
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# NEXTAUTH_URL=
